{
  "name": "store-rating-platform",
  "version": "1.0.0",
  "description": "FullStack Store Rating Platform",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
